/**
 * @file
 * visita_ubec_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.visitaUbecTheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
